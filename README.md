# Python Calculator

A simple calculator that runs in the terminal. Built in Python as a beginner project.

## Features

- Addition, Subtraction, Multiplication, Division
- Handles bad input without crashing
- Keeps running until you choose to exit
- Division by zero protection

## Requirements

- Python 3.x

## Notes

- Results show as decimals (`2.0` instead of `2`)
